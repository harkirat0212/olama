# Core
streamlit>=1.25.0
requests>=2.28.0

# Ollama client
ollama>=0.6.1

# LangChain: use a modern single version (no conflicting old pins)
langchain>=1.1.0

# LangChain Ollama integration
langchain-ollama>=1.0.0

# Vectorstore & PDF helpers
chromadb>=1.3.5
pypdf>=3.6.0

# Optional (only if you need local embedding models or OCR)
sentence-transformers>=2.2.2   # optional: not required for HF Inference path
pdf2image                      # optional (OCR)
pytesseract                    # optional (OCR)
